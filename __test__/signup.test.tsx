import { render, screen } from "@testing-library/react";
import { SignupForm } from "@/components/SignupForm";

// Tests for SignupForm component
describe('Test SignupForm Component', () => {

    test('should have the header', async () => {
        
    })

    test('with valid email on email validation', () => {

    })

    test('with invalid email on email validation', () => {

    })

    test('with valid password on password validation', () => {

    })

    test('with invalid password on password validation', () => {
        
        
    })
})