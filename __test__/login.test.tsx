import { render, screen } from '@testing-library/react'
import { LoginForm } from '@/components/LoginForm'

describe('Test LoginForm Component', () => {
    test('should have the header', () => {

    })

    test('with ', () => {
        
    })

    test('with valid email on email validation', () => {
        
    })

    test('with invalid email on email validation', () => {
        
    })

    test('with valid password on password validation', () => {
        
    })

    test('invalid password on password validation', () => {
        
    })

    test('should have buttons', async () => {
        
    })
})